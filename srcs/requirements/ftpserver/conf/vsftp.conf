# Basic configuration for vsftpd

# Allow anonymous FTP access
anonymous_enable=NO

# Disable local user access (anonymous only)
local_enable=YES
write_enable=YES
local_umask=022

# Set welcome message
ftpd_banner=Welcome to the FTP Server! Enjoy your stay.

# Connection settings
listen=YES